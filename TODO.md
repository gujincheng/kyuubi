# TODO

## SQL 执行记录

- [x] 开发 SQLRecord 执行记录功能
  - 支持按时间、用户、Session、Engine、状态筛选
  - 支持分页查询和 SQL 摘要展示
  - 展示执行耗时、队列等待时间、状态和错误信息
  - 支持查看 SQL 详情及失败原因
  - 支持从 Overview 的失败率、执行趋势和队列指标下钻
  - 已完成单元测试、REST 测试、真实 SQL 接口验证和浏览器页面验证

## Overview 监控首页

- [x] 指标口径统一与接口校验
  - 对齐 Kyuubi Metrics 中的 Server、Engine、Session、Operation、执行队列指标
  - 明确 `size`、`active`、`waiting`、`open`、`running`、`pending` 的统计口径
  - 确保失败率、延迟分位数和趋势数据不重复计算
  - 增加接口级断言，保证零值、空指标和异常指标有稳定返回
  - 已完成队列指标单次快照读取，`size` 与 `waiting` 统一来自同一个 Kyuubi 执行队列指标
  - 已通过 `OverviewResourceSuite`（4/4）、真实 REST 接口、真实 SQL 执行及浏览器端到端核验
- [x] 空数据、异常数据和失败状态处理
  - 区分无数据、数据采集中、数据过期和服务异常
  - 为趋势图和统计卡片提供明确的空状态说明
  - 验证接口异常时页面不会展示误导性数据
  - 趋势范围仅允许 `1h`、`1d`、`7d`，非法范围返回 HTTP 400
  - 增加 `INITIALIZING`、`READY`、`STALE` 数据状态及更新时间提示
  - 趋势接口失败时保留上一次数据，并在页面展示告警，不用零值覆盖
  - 已通过前端单测（9/9）、`vue-tsc`、`OverviewResourceSuite`（5/5）
  - 已通过真实 REST 接口、成功 SQL、失败 SQL、临时会话清理和浏览器端验证
- [x] Overview 端到端验证
  - 验证空闲、成功 SQL、失败 SQL、并发排队四种场景
  - 对账页面展示值与接口及 Metrics 原始值
  - 验证刷新、时间范围切换和下钻到 SQLRecord
  - 已验证默认空闲状态：活动用户、Session、操作和队列均回到 0，健康状态为 NORMAL
  - 已验证成功 SQL：`FINISHED_STATE`、成功趋势点、P95 延迟、Spark SQL Engine 次数和 SQLRecord 一致
  - 已验证失败 SQL：`ERROR_STATE`、失败计数、失败率、失败趋势点和健康告警一致
  - 已用单线程临时实例验证并发提交、队列等待耗时和 SQLRecord 记录；验证后已恢复默认线程池
  - 已对账 REST Summary、`/metrics` 原始值及浏览器展示，验证手动刷新、`7D` 切换和下钻 SQLRecord
- [x] Overview UI 最终收口
  - 优化卡片层级、间距、告警状态和响应式布局
  - 趋势图铺满容器，完善坐标轴、Tooltip、空状态和图例
  - 统一 `0 / 0`、百分比、耗时和 Engine 展示规则
  - 趋势图高度收紧为 208px，保留全宽绘制、坐标轴、Tooltip、空状态和图例
  - 增加趋势数据状态徽标：数据正常、数据采集中、数据已停止更新
  - 窄屏下趋势操作区纵向排列，避免状态徽标、下钻和时间范围按钮拥挤
  - 未知 Engine 类型保留后端原始类型，不展示 `-`
  - 已通过 ESLint、前端单测（10/10）、`vue-tsc`、生产构建及桌面/窄屏浏览器验证

## SQLRecord 执行记录增强

- [x] 执行记录自动刷新和实时状态更新
  - 支持每 5 秒、每 10 秒、每 30 秒和关闭自动刷新
  - 自动刷新期间保留当前筛选条件、分页和已有记录
  - 刷新失败时保留旧数据并展示可关闭的告警，不用空结果覆盖页面
  - 展示最近更新时间，支持手动刷新
  - 已通过 SQLRecord 单测（4/4）、ESLint、`vue-tsc` 及真实浏览器 SQLRecord 验证
- [x] 执行记录导出
  - 按当前筛选条件导出 CSV，字段包含 SQL、用户、Session、Engine、状态、时间、队列等待、执行耗时和错误原因
  - 导出重新拉取第 1 页最多 200 条，避免只导出当前分页造成误解；超过上限时明确提示
  - CSV 使用 UTF-8 BOM、字段转义和换行清理，兼容 Excel 和常见数据工具
  - 已通过 SQLRecord 单测（5/5）、ESLint、`vue-tsc` 及真实浏览器导出请求验证
- [x] 记录保留、容量上限和清理策略
  - 进程内执行记录最多保留 10000 条，超出后按最早记录淘汰
  - 执行记录按 24 小时保留期清理，写入、列表查询和详情查询都会触发清理
  - 服务重启时清空进程内历史，避免把临时内存记录误认为持久化审计数据
  - 已通过 Store 及 REST 定向测试（5/5），覆盖过期清理、容量淘汰和接口查询
  - 已通过重新编译服务后的真实 REST 冒烟验证：SQLRecord 返回空闲状态、Overview Summary 返回 READY、Prometheus `/metrics` 可访问
- [x] 失败原因、队列等待和执行耗时详情增强
  - 列表新增执行诊断列，区分错误、执行中、队列中和无错误记录
  - 详情展示总耗时、队列等待、执行耗时及创建/排队/开始/完成时间线
  - 运行中记录的总耗时按当前时间估算，已完成记录按服务端时间戳计算
  - 未捕获失败原因时展示明确提示，避免把空错误误解为执行成功
  - 已通过前端单测（6/6）、ESLint、`vue-tsc`、生产构建
  - 已通过真实浏览器成功/失败 SQL 端到端验证：成功记录显示无错误，失败记录显示错误原因、队列等待、执行耗时、总耗时和时间线

## 管理能力

- [x] 数据源管理
  - 新增现代化 Data Source 页面，支持 JDBC 与 Iceberg 数据源新增、编辑、详情、启停、删除、搜索和类型/状态筛选
  - 提供 StarRocks、MySQL、PostgreSQL、Oracle、SQLite 和通用 JDBC 模板，统一维护驱动、URL、账号、凭据及连接池参数
  - Iceberg 数据源统一维护 Hive Metastore、Warehouse、S3 Endpoint 和 Session Profile；会话只传数据源标识，网关自动切换 Spark SQL Engine 并注入 Catalog 配置
  - 以数据源及最终配置指纹隔离 Spark Engine，避免不同 Iceberg Catalog 或资源规格错误复用同一引擎
  - 新增保存前测试和已保存数据源测试，返回数据库产品、版本、耗时及明确失败原因；停用数据源仍允许管理员执行连接诊断
  - 密码仅提交到服务端加密存储，REST 不返回明文或密文；编辑时留空保持原凭据，新建空密码不生成伪凭据
  - JDBC URL 禁止内嵌 password/passwd/pwd，连接池参数使用白名单和范围校验，避免覆盖 Kyuubi 保留配置
  - 已通过插件测试 15/15、REST 集成测试 9/9、前端单测 7/7、真实 SQLite 持久化对账和浏览器端到端验证
  - 已从页面创建 Iceberg 数据源并连接 `172.16.7.137:9083`，通过 Beeline 完成建库、建表、写入、查询和清理；查询返回 `2 / created-from-page`
- [x] Session、Operation、Engine、Server 管理页面补齐真实操作和状态展示
  - [x] Session 管理页面：真实筛选、自动刷新、状态/统计展示、详情抽屉和关闭 Session
  - 已通过 Session 页面单测（5/5）、`vue-tsc`、ESLint 和生产构建
  - 已通过真实浏览器端到端验证：创建临时 Session、按用户筛选、查看详情、确认关闭，关闭后列表和统计回到 0，浏览器错误日志为空
  - [x] Operation 管理页面：用户/Session/状态筛选、自动刷新、状态统计、详情抽屉和取消/关闭操作
  - 已通过 Operation 页面单测（5/5）、`vue-tsc`、ESLint 和生产构建
  - 已通过真实浏览器端到端验证：提交异步 SQL、查看真实 Operation 详情、按用户筛选、关闭 Operation，随后关闭临时 Session，列表和统计正确刷新，浏览器错误日志为空
  - [x] Engine 管理页面：真实筛选、在线状态/类型/用户统计、自动刷新、详情抽屉、Engine UI 入口和移除操作
  - 已通过 Engine 页面单测（4/4）、`vue-tsc`、ESLint 和生产构建
  - 已通过真实浏览器端到端验证：真实拉起 Engine、核对 Engine ID/地址/版本/状态、查看详情、确认移除，列表和统计回到 0，浏览器错误日志为空
  - [x] Server 管理页面：主机/状态筛选、节点健康统计、自动刷新、详情抽屉和节点属性展示
  - 已通过 Server 页面单测（4/4）、`vue-tsc`、ESLint 和生产构建
  - 已通过真实浏览器端到端验证：核对 `/api/v1/admin/server` 节点地址、Running 状态、版本、命名空间和属性详情，按主机筛选成功，浏览器错误日志为空
- [x] 用户、Profile、黑白名单管理
  - 新增管理员权限保护的 `GET /api/v1/admin/policies`，统一返回 Profile、用户默认配置、无限制用户、拒绝用户和拒绝 IP
  - 新增管理员权限保护的 `PUT /api/v1/admin/policies`，支持用户默认配置、Session Profile、无限制用户、拒绝用户和拒绝 IP 的新增、修改、删除
  - 配置文件原子写入，校验用户、Profile 名称、属性键值和访问控制值，避免越权写入任意路径
  - 敏感配置值脱敏展示；Profile 显示配置文件、配置项数和修改时间；页面明确配置文件驱动并提供编辑/删除反馈
  - 接入已有四类刷新接口，支持单域刷新和全部策略刷新
  - 新增 Policy 管理页面，包含访问控制、Session Profile、用户默认配置三个页签及空态/错误态/刷新反馈
  - 已通过 `AdminPoliciesFileStoreSuite`：3/3；后端 server 依赖编译通过；前端 Policy 单测：4/4；`vue-tsc`、ESLint、生产构建
  - 已通过隔离端口实例真实 REST 验证：用户默认配置、Session Profile、黑白名单新增/修改/删除均返回 200，删除后运行时查询状态同步清空
  - 已通过真实浏览器端到端验证只读加载、三个页签切换和刷新；策略编辑/删除链路已由隔离实例 REST 对账验证
- [x] 系统配置和权限管理
  - 新增管理员权限保护的 `GET /api/v1/admin/configuration`，返回安全模式、认证方式、管理员、配置分类和运行时配置快照
  - 配置项按 Key 分类并对 password、secret、token、credential 等敏感值统一脱敏；页面明确只读、配置文件驱动
  - 接入 Hadoop、Kubernetes、User defaults、Access policies 四类受控刷新动作，不开放任意配置写入
  - 新增现代化 System Setting 页面，支持配置分类筛选、Key/Value 搜索、刷新反馈、权限快照和响应式布局
  - 已通过后端 `AdminResourceSuite`：20/20，覆盖管理员鉴权、敏感值脱敏和既有管理接口回归
  - 已通过真实 REST：配置快照返回结构正确，Hadoop 配置刷新返回成功；本地 `NONE` 安全模式下匿名请求符合 Kyuubi 行为
  - 已通过真实浏览器端到端验证：页面真实数据加载、受控刷新、更新时间和成功提示，浏览器错误日志为空
  - 已通过前端配置页单测 4/4；前端全量单测 20 个文件、126/126 用例通过
  - 已通过 ESLint、`vue-tsc` 和生产构建；测试环境补充有效 `localStorage` 隔离实现
- [x] 管理审计
  - 复用现有 `AuthenticationAuditLogger`，记录 HTTP/Thrift 请求的用户、认证方式、客户端 IP、代理链、请求方法、URI、查询参数、协议和响应状态
  - 新增有界审计存储：最多 2000 条、保留 24 小时，支持 JSONL 文件持久化；通过 `KYUUBI_AUDIT_LOG_PATH` 指定文件，未指定时使用 `$KYUUBI_HOME/logs/kyuubi-audit.jsonl`
  - 共享文件使用文件锁和实例间重新加载，支持多个 Kyuubi Server 进程读取同一份审计记录；敏感查询参数统一脱敏
  - 新增管理员接口 `GET /api/v1/admin/audit`，支持用户、请求方法、状态码、时间范围和条数筛选
  - 管理员策略新增、修改、删除写入 `ACTION` 审计记录，页面展示具体动作标识
  - 新增现代化只读 Management Audit 页面，以管理员动作、操作对象、操作者、客户端 IP 和结果为主视角
  - 后端审计存储测试 4/4 通过，覆盖持久化加载、脱敏、过期清理和管理员动作记录；管理 REST 测试覆盖鉴权和筛选
  - 已通过真实 REST 验证：接口结构、请求记录、筛选和 `password=******` 脱敏均正确
  - 已通过真实隔离实例端到端验证：策略 PUT 返回 200 并产生 `policy.access.replace` 动作记录；服务重启后按 `method=ACTION` 仍可读取同 2 条记录
  - 已通过真实浏览器端到端验证：页面加载、刷新、用户筛选、真实数据展示和浏览器错误日志检查
  - 已通过前端审计页单测 4/4；前端全量单测 20 个文件、126/126 用例通过
  - 已通过 ESLint、`vue-tsc` 和生产构建；浏览器端到端验证记录在上方
- [x] Kyuubi 原生 Audit Log
  - 复用 Kyuubi `KyuubiEvent` 与原生 JSON/Kafka EventHandler，采集 Server、Session、Operation 和 SQL 拦截事件，不重复计算 SQL 生命周期
  - 新增 Audit Log 页面，可选择 JSON 文件或 Kafka，配置存储参数、测试连通性、启停审计并立即生效，无需重启 Server
  - JSON 查询直接读取事件目录；Kafka 查询直接消费 Topic；切换存储方式不会迁移或删除原存储中的历史事件
  - Kafka 支持 PLAINTEXT、SSL、SASL_PLAINTEXT、SASL_SSL 及 PLAIN/SCRAM，密码加密保存，接口和原始事件统一脱敏
  - 新增 `/api/v1/admin/event-audit/config`、`/test`、`/events` 管理接口，并将配置变更写入管理审计
  - 后端定向测试 8/8、前端定向测试 5/5、Spotless、ESLint、类型检查和生产构建通过
  - 已完成 JSON 与 Kafka 真实端到端验证：Beeline SQL 返回正确结果，页面接口按同一 operationId 读取 INITIALIZED、PENDING、RUNNING、FINISHED、CLOSED 五个原生状态
  - 已使用本机 Chrome 真实渲染验证记录列表、状态徽标、空值展示和后端真实数据加载

## 细粒度管理员权限

- [x] 角色与权限模型
  - 内置只读管理员和平台管理员两类角色；运维和策略管理统一由平台管理员负责
  - 按资源和操作拆分 `read`、`write`、`control`、`delete`、`refresh`、`manage`
  - 保留 `SERVER_ADMINISTRATORS` 作为平台管理员兼容入口；未启用安全认证时保持原有全放行行为
- [x] 权限策略持久化与安全边界
  - 使用 `kyuubi-admin-permissions.json` 原子写入，支持 `KYUUBI_ADMIN_PERMISSIONS_PATH` 指定路径
  - 校验用户名、角色和重复分配，禁止把最后一个平台管理员全部移除
  - 引擎查询/删除保留 Kyuubi 原有 proxy user 校验语义，避免破坏普通用户引擎管理
- [x] 管理 REST API 与资源鉴权
  - 新增 `GET/PUT /api/v1/admin/permissions`
  - 为策略、配置、审计、SQL 规则、数据源、Session、Operation、Batch 和 Server 管理接口接入资源级鉴权
  - 权限变更写入 `ACTION` 审计记录
- [x] 权限管理前端工作台
  - 展示角色卡片、权限矩阵、用户角色分配、当前用户和存储状态
  - 支持角色新增、修改、移除、加载失败提示、保存反馈和响应式布局
  - 已通过前端单测 4/4、全量前端单测 21 个文件/130 个用例、ESLint、`vue-tsc` 和生产构建
- [x] 端到端与回归验证
  - `AdminPermissionStoreSuite` 3/3 通过
  - `AdminResourceSuite` 23/23 通过，覆盖只读角色可读不可刷新、权限接口写保护和既有引擎 proxy user 行为
  - 已通过真实 HTTP 隔离实例验证：未授权 401、viewer 读取 200、viewer 刷新 403、viewer 修改权限 403、全局管理员更新 200
  - 已通过真实重启验证：viewer 角色分配写入后，服务重启仍可读取

## 企业访问管理

- [x] LDAP / IAM 身份源接入
  - 统一维护 LDAP 与公司 IAM 的服务地址、目录/接口映射、超时和启停状态
  - 服务账号密码、Bearer Token 仅引用服务端环境变量，不在页面、接口响应或配置文件中保存明文
  - 支持身份源连通性测试、LDAP 用户/用户组检索和 IAM 用户/用户组查询
- [x] 外部账号访问授权
  - 外部系统继续作为账号主数据源，Kyuubi 只保存外部主体与本地访问策略的绑定关系
  - 支持启用/拒绝访问、管理员角色、Session Profile、免配额和用户默认配置绑定
  - 绑定变更实时投影到管理员权限、用户黑白名单、Session Profile 和用户默认配置
  - 删除身份源前检查关联绑定，避免产生悬空授权；统一认证仅允许已启用绑定的用户登录
- [x] 统一认证启用
  - 新增 Kyuubi CUSTOM 认证适配器，按账号绑定选择已启用的 LDAP / IAM 身份源进行密码验证
  - 启用前检查至少存在一个已启用身份源和有效的平台管理员授权，防止重启后管理员被锁在系统外
  - 写入认证类和管理员配置后明确提示重启生效，不在运行中隐式切换认证链路
- [x] 访问管理前端整合
  - 将原 Policy 与权限管理整合为“访问管理”，包含认证接入、授权详情、会话模板三个页签
  - 授权详情统一承载账号筛选、目录授权、访问状态、管理员角色、Session Profile 和 IP 拒绝名单，不再展示内部权限操作矩阵
  - 旧 Policy、Permissions 路由保留兼容跳转，侧边栏只保留一个访问管理入口
  - 已通过访问管理单测 8/8、ESLint、`vue-tsc` 和生产构建
- [x] LDAP / IAM 联调与端到端验收
  - 后端测试启动真实内存 LDAP 和轻量 IAM HTTP 服务，覆盖目录查询、用户组查询、正确/错误凭据及多身份源认证
  - Store、Directory、REST 三组测试共 9/9 通过，覆盖身份源 CRUD、绑定投影、解绑清理、权限保护和认证启用
  - 已通过真实 Kyuubi Server 与浏览器联调：页面加载、页签、旧路由、新增弹窗和认证安全提示均正常，浏览器控制台 0 错误
